export class Offer {
    constructor (
        public id: string, 
        public title: string, 
        public publishedAt: string, 
        public lon: number, 
        public lat: number,
        ) {
    }
}